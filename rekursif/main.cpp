#include <iostream>
#include "tiling_recursive.h"
int main() {
    int n; // Ukuran papan
    cout << "Masukkan ukuran papan (N x N)(Rekursif): ";
    cin >> n; // Mengambil input dari pengguna
    test_algorithms(n);
    return 0;
}
